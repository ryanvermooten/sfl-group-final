<%= form_for([@group, @gardener, @sfl]) do |g| %>
  <div class="panel panel-default">
    <%= render "shared/error_messages", :target => @sfl %>
    
  <div class="btn-group btn-group-justified">
    <h4> 1.)how did you hear about Soil For Life? </h4> 

        <%= g.radio_button :referral, :friend_or_neighbour %> 
        <%= g.label :referral, " A friend, family or neighbour" %> <br/>
      
        <%= g.radio_button :referral, :sfl_trainer %>
        <%= g.label :referral_sfl_trainer, " A Soil For Life trainer" %>
         <br/>

        <%= g.radio_button :referral, :sfl_event %>
        <%= g.label :referral_sfl_event, " A Soil For Life Event" %>
  </div>

  <div class="field">
    <h4> 2.) Have you revieved help from any other organizations? </h4>

        <%= g.check_box :other_organizations %>
  </div>

    <div class="field">
      <h4> 2.1) Which other organizations have you recieved help from? </h4>

        <%= g.text_field :other_organizations_qualitative %>
   </div>

    <div class="field">
      <h4> 3.) Why do you want to learn how to grow vegetables? </h4>
          
          <%= g.text_field :ambition %>
    </div>

    <div class="form-actions">
         <%= g.submit 'continue', :class =>  'btn btn-lg btn-success' %>
    </div>

</div>
<% end %>